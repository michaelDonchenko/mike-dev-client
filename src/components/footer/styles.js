import { makeStyles } from '@material-ui/core'

const styles = makeStyles({
  linkDark: {
    marginLeft: '3px',
    textDecoration: 'none',
    color: 'white',
    marginLeft: '5px',
  },

  linkLight: {
    marginLeft: '3px',
    textDecoration: 'none',
    color: 'black',
    marginLeft: '5px',
  },
})

export default styles
