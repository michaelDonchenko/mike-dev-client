import { createMuiTheme } from '@material-ui/core'

const lightTheme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#00bcd4',
    },
    secondary: {
      main: '#26a69a',
    },
  },
})

export default lightTheme
