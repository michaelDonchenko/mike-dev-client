import { createMuiTheme } from '@material-ui/core'

const darkTheme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#ff165d',
    },
    secondary: {
      main: '#fff',
    },
  },
})

export default darkTheme
